# Проверка прототипа

Среда: Windows, Unreal Engine 5.8.1, MSVC 14.44, Windows SDK 10.0.22621, Blender 5.1.0.

## Кофейная поверхность и ragdoll-плавание — 25.09.2026

Editor и Game Development собраны. **20 Automation-тестов прошли без предупреждений.** Новый `Coffee.SurfaceAndRagdollSwimming` проверяет профиль/ассеты, ограниченную высоту волн и силу, сопротивление, затем запускает настоящие физические тела двух игроков. За три секунды подгребания они сместились примерно на 3,4 м в противоположные стороны, остались около поверхности и не смогли встать до слива. Для этого теста гребень волны удерживается неподвижным, течение выключено.

`CoffeeNetwork -PacketLagMs 75 -PacketLoss 2 -CaptureCoffee` прошёл на хосте и трёх клиентах: все получили `seen=127`. Проверены профиль, четыре волны, зацеп первого игрока, ragdoll и подгребание второго, удержание около поверхности, слив и подъём. Это эмуляция локальной сети; точность совпадения каждой кости между машинами и WAN не измерялись. В сетевых логах остаются предупреждения инициализации PhysicsControl у временного рига; нечисловых координат тест не обнаружил.

Запись [CoffeeWater.mp4](../Artifacts/CoffeeWater.mp4): реальный Unreal, 1280×720, H.264, 25,2 секунды, без звука. Камера и действия участников заданы сценарием; физика рассчитывается движком. Исходный захват — 91 кадр, примерно 3–4 кадра/с; файл кодируется в 30 FPS с сохранением времени, поэтому видео заметно ступенчатое. Это демонстрация поведения, не измерение FPS обычной игры. Четыре волны в записи растянуты до 20 секунд; штатные 10 секунд в DA не изменены.

Визуально проверены первая волна, пена и кольца возле игроков, прозрачность и отсутствие воды после слива. Исправлено скрытие поверхности во время подготовки первого PSO: для компонента кофе render proxy создаётся сразу, сохраняя предварительное кеширование. На холодном запуске это может дать паузу подготовки вместо невидимой физической воды. Ошибок компиляции материала в итоговом прогоне нет. Сетка создана Blender 5.0.1; карта и модели коллеги сохранены. [Настройки и ограничения](CoffeeWater.md).

## Dev-панель — 25.09.2026

Editor и Game Development собраны успешно. Упакованная Shipping-сборка не запускалась.

**19 Automation-тестов прошли без предупреждений**, включая `Development.EventSandbox`: доступ хоста, отказ неизвестному контроллеру/несуществующему этапу, прямой запуск уборки с едой, отсутствие автоперехода при выполнении задачи, кофе, инфекция через реальную порчу, сохранение урона от язвы, очистка при повторном запуске, гибель с расходом конкретного зуба и возвращение в обычный день.

`DevPanelNetwork -PacketLagMs 75 -PacketLoss 2 -CaptureDevPanel` прошёл в четырёх отдельных процессах: хост и три клиента увидели еду, язву, воду, налёт, застрявшие волокна, расход одного зуба и сброс. Все три клиента подтвердили запрет управления панелью. Это локальная эмуляция сети. Проверен реальный [скриншот F3](../Artifacts/DevPanel.png), полученный из Unreal при сетевом запуске.

При проверке обнаружено, что после нового блокинга в `L_Mouth` осталось два `MCArenaToothSocket`, 01 и 02. Правила по-прежнему требуют восемь. Панель показывает фактический запас и пояснение; сетевая проверка проверяет изменение запаса на один и его восстановление, а не предполагает восемь маркеров в любой карте. Unit-тест отдельно подтверждает запас 8 → 7 → 8. Геометрия и маркеры карты этой доработкой не менялись. [Инструкция и устройство панели](DevPanel.md).

## Риг коллеги — 25.09.2026

Подключён `SM_Teeth_rig` с 108 костями через `DA_PlayerAppearance`. Исходные ассеты коллеги сохранены. Для игры создан `PA_TeethPlayer`: 13 физических тел, 12 суставов с ограничениями. Сборки Editor и Game Development успешны; **18 Automation-тестов прошли без предупреждений**, включая `ArtistRigIntegration` (реальный меш игрока, карта костей, направление вперёд, Physics Asset, крепление щётки, параметры кофе/урона и включение симуляции корпуса).

`Limbs` при 60 FPS прошёл: максимальный угол 38,78°, вращение в покое 4°/с, отклонение после подъёма 0,05°, времени с некорректной позой — 0. `RagdollVisual` подтвердил удар и подъём; проверены кадры [стойки](../Artifacts/PlayerRig/Rig.png), [удара](../Artifacts/PlayerRig/Ragdoll.png) и [подъёма с кофе](../Artifacts/PlayerRig/Recovered.png). Ошибок компиляции игровых материалов не обнаружено.

`Ragdoll -PacketLagMs 75 -PacketLoss 2` прошёл в четырёх отдельных процессах: падения и подъём наблюдались хостом и всеми клиентами, недопустимых поз не зарегистрировано. Это локальная проверка с эмуляцией сети; упакованный билд и WAN не проверялись. Оставшиеся ограничения рига описаны в [PlayerAppearance.md](PlayerAppearance.md).

`DayOneNetwork -PacketLagMs 75 -PacketLoss 2 -CaptureDayOne` также прошёл во всех четырёх процессах: каждый наблюдал все восемь проверяемых признаков, включая ragdoll в кофе и зацеп за зуб. Запись с новой моделью: [DayOneCoop.mp4](../Artifacts/PlayerRig/DayOneCoop.mp4).

## Первый день — 25.09.2026

Editor и Game Development собраны. Все **17 Automation-тестов прошли без предупреждений**. Новые проверки: `DayOne.SequenceAndNoGlobalDeadline`, `PhysicalBrushAndCorrectDisposal`, `MenuFragmentsMassAndSpoilage`, `UlcerProtectionAndCoffeeControl`. Проверяются этапы внутри дня, отсутствие общего дедлайна, очистка зубов и поверхности, разные места утилизации щёток и еды, варианты мешей из таблицы, сохранение массы и срока порчи при разрушении, язва на настоящей коллизии пола, автоматическое заживление и его сброс при наступании, серверное ограничение подгребания и отключение воды после события.

`Tools/Test.ps1 -Mode DayOneNetwork -PacketLagMs 75 -PacketLoss 2` прошёл на listen server и трёх отдельных клиентах: каждый увидел физический подбор щётки, чистку поверхности, бросок за борт, разрушение брокколи, инфицированные обломки и язвы, ragdoll в кофе, зацеп первого игрока за большой зуб и четвёртую волну. Проверка использует обычные игровые RPC и Chaos. В тестовом сценарии участники расставляются у целей; это интеграционная проверка механик, а не автоматическое прохождение полного уровня с навигацией. Исходные логи этого прогона сохранены локально как `Saved/Logs/DayOneLag0.log`…`3.log`.

В ходе проверки исправлены размещение тестовых пятен под поверхностью пола и блокирование выбрасываемых щёток невидимой передней стенкой. Новый объектный канал `BrushTool` позволяет только щёткам проходить через эту стенку у маркера выхода. Коллизия игроков и еды остаётся прежней. Модели еды созданы Blender 5.0.1; исходники и генераторы включены в репозиторий.

Подробнее о настройках и границах прототипа — [Day01.md](Day01.md). Симуляции разлива по произвольной геометрии, точного разреза и IK рук пока нет; текущие фрагменты выбираются из заранее подготовленных вариантов. Пенная вечеринка не реализована. Упакованный билд и игра через интернет не подтверждаются этими проверками.

Повторный `CoreNetwork` с 75 мс / 2% также прошёл во всех четырёх процессах: прежние чистка, уход, смерть/возрождение, совместное извлечение и переноска продолжают работать. `DayOneNetwork` с графикой тоже прошёл. Запись [Day01_Mechanics.mp4](../Artifacts/Day01_Mechanics.mp4): H.264, 1280×720, 30,33 секунды, 30 FPS после перекодирования кадров с сохранением времени, без звука. Визуально проверены чистка, обломки, красные язвы и полупрозрачный кофе с игроками в ragdoll и зацепом. При первой записи найдена и исправлена ошибка соединения Time → Sine в материале; в итоговой записи нет ошибок компиляции материала. Это сценарная демонстрация на блокинге, а не финальная графика или полное прохождение дня.

## Повторные толчки от лежащей еды — 25.09.2026

Исправлен расчёт удара: скорость игрока и разделяющий импульс решателя больше не превращают касание неподвижной еды в атаку. Проверяются скорость самой еды до физического шага в сторону контакта и скорость сближения. Предмет также не атакует своих держателей; обычная коллизия остаётся.

Editor и Game Development собраны. Все **13 Automation-тестов прошли без предупреждений**. Новый `RestingFoodDoesNotLaunchPlayers` проверяет набегание на неподвижную еду, движение еды от игрока и по касательной, медленный толчок, расходящиеся тела и контакт с держателем. Проверки используют настоящий hit delegate с заданными скоростями и большим контактным импульсом; быстро летящая навстречу свободная еда всё ещё должна наносить урон и включать ragdoll. Отдельный `CoreNetwork -PacketLagMs 75 -PacketLoss 2` прошёл во всех четырёх процессах с настоящим падением еды, ragdoll, возрождением, совместным извлечением и переноской до утилизации.

## Кофе, лечение, возрождение и еда — 25.09.2026

Новая реализация описана в [FirstFourSystems.md](FirstFourSystems.md). Собраны Editor и Game Development. Все **12 тестов прошли без предупреждений**: прежние проверки событий переведены на реальные зубы/еду; добавлены `SharedStatusAndCare`, `ConcreteReserveRespawn`, `FoodGripPullAndImpact`. Обычный `Network` со случайными днями также прошёл во всех четырёх процессах; участники завершили первый день и перешли ко второму.

Проверяются отдельные завершённые контакты по 0,5 с, сброс прерванного контакта, совместная чистка, неподходящий инструмент и направление, уход за собой, настройки из данных, запрет лечения погибшего, одновременный расход разных зубов, наследование состояния и отсутствие повторного списания. Проверка еды включает дистанцию/разрыв хвата, направление вытягивания, утилизацию и защиту от повторных hit callbacks.

`CoreNetwork` прошёл в четырёх отдельных процессах, в том числе с эмуляцией **75 мс задержки и 2% потерь**. Каждый процесс увидел кофе и очистку, расшатывание и лечение, ragdoll от реально падающей еды, расход зуба, два хвата одного предмета, извлечение, переноску и утилизацию. Действия выполняются через обычные RPC игроков. Сервер дополнительно проверяет участие обоих чистящих игроков. Флаг `-MCCore` не включается в обычной игре.

Запись этого же сетевого сценария из Unreal: [Step03_06_Gameplay.mp4](../Artifacts/Step03_06_Gameplay.mp4), 1280×720, 35,83 секунды, без звука. Камера и команды участников заданы сценарием. Проверены кадры с налётом, уменьшением числа контактов, расходованным зубом и физической переноской. Материалы скомпилировались без ошибок. Видео захватывается примерно с 10 кадрами/с, затем кодируется в 30 кадров/с с исходным временем.

При записи выявлено отставание физики еды от CharacterMovement на низком FPS. Включены подшаги физики (1/120 с, до 16 шагов); после исправления вся последовательность снова прошла на сервере с графикой и трёх клиентах. Отдельная регрессия `Limbs -FrameRate 30` прошла: максимальный угол 58,20°, скорость руки в покое 0,04°/с, некорректных кадров нет, падение/подъём выполнены.

Ниже сохранена история проверок более ранних этапов. Она не означает наличие новых систем в старых видеозаписях.

Уточнение размещения зубов (25 сентября 2026): игровые зубы перенесены на места крупных нижних моляров; по выбору пользователя оставлены четыре слева и четыре справа. Дополнительного внутреннего ряда нет. Editor/Game Development собраны, **9 Automation-тестов прошли без предупреждений**. Новый `ArenaAuthoredPlacement` проверяет использование точного transform/модели редакторского размещения, стабильный ID, отсутствие дополнительных зубов при наличии размещения, скрытый предпросмотр и восстановление после перезапуска. Сетевой `ArenaNetwork` с 75 мс / 2% снова прошёл во всех четырёх процессах; начальная проверка теперь требует большой размер зуба и положение в боковом ряду. Обновлена карта и видео; визуально проверены крупный кофейный зуб и свободное место после выпадения. Запись ждёт завершения компиляции шейдеров перед прогревом и захватом кадров.

Шаг 2 (`MCArenaTooth`) проверен 25 сентября 2026: Editor Development и Game Development собраны. Восемь Automation-тестов прошли без предупреждений. Новые `ArenaToothLifecycle` и `ArenaBrushHit` проверяют реальные восемь акторов/ID, отдельное здоровье, сохранение повреждений/налёта между днями, однократную потерю, отсутствие дубликатов при перезапуске и один урон от одного разрешённого удара щётки.

`ArenaNetwork -PacketLagMs 75 -PacketLoss 2` прошёл во всех четырёх отдельных процессах: каждый увидел начальные 8 зубов, 25 HP + кофе + расшатывание, затем потерю и 7 доступных зубов. Одновременно прошла обычная проверка совместной работы над заданиями. Первый прогон выявил, что тест начинал повреждение до получения начального состояния последним клиентом; сценарий исправлен паузой после подключения четвёртого игрока. Сетевая проверка подтверждает состояния и конечные координаты без NaN, не измеряет точность физической позы между клиентами и не заменяет испытание через интернет.

Записан и визуально проверен `Artifacts/Step02_ArenaTeeth.mp4`: H.264, 1280×720, 30 FPS, 690 кадров, 23 секунды, без звука. Кадры целого зуба, кофейного слоя, повреждения и выпадения проверены после исправления Vertex Interpolator в материале. Ошибок компиляции материала в итоговом рендере нет. Это сценарная демонстрация в Unreal на временной геометрии; числа/события задаёт `MCArenaDemo`, движение выпавшего зуба — физика. Сам процесс записи не оценивает визуальное качество автоматически; скрипт проверяет завершение, число кадров и ошибки компиляции материалов.

Шаг 1 (`DA_RunRules`) проверен 25 сентября 2026: Editor Development и Game Development собраны, шесть Automation-тестов прошли без предупреждений. Новый тест использует временный профиль на 40 здоровья / два дня / два игрока: подтверждены победа по изменённому числу дней, предел лечения, отказ третьему игроку и применение изменений только на следующем старте. Отдельный `Network` на штатном профиле 100 / 7 / 4 прошёл во всех четырёх процессах. Это проверка приёма игроков и обычной сетевой игры; отдельного прогона сети с нестандартным профилем здесь не было.

Проверено 25 сентября 2026: Editor Development и Game Development собираются; пять Automation-тестов прошли без предупреждений. Listen server и три отдельных клиента прошли проверку ударов, ragdoll, подъёма и дальнейшей совместной работы: 20 падений/подъёмов без эмуляции, 17 с `PktLag=75` и `PktLoss=2`. Каждый процесс наблюдал переходы; проверка не обнаружила нечисловых координат или разлёта костей дальше 250 см от тела. Это не измерение ошибки каждой позы или качества WAN. Интернет-сессия между разными компьютерами и packaged build пока не проверялись.

После исправления вращения рук повторно прошли обе сборки, пять Automation-тестов и `Limbs` с ограничением 30 / 60 FPS. Максимальное отклонение от исходной стойки после подъёма — 0,19° / 0,21°, времени с выворотом >135° — 0. Последний прогон 60 FPS выполнен после интеграции обновлённой карты и моделей из `TEETH_TEST`. Отдельный запуск с рендерингом тоже прошёл (отклонение 0,23°); кадры после подъёма и завершения предпросмотра проверены визуально.

Повторный сетевой прогон с `PktLag=75` / `PktLoss=2`: все четыре процесса прошли, сервер наблюдал 21 падение и 21 подъём, некорректных положений костей не обнаружено. При старте одного клиента зафиксированы шесть предупреждений Physics Control о временно отсутствующих телах; далее ограничения восстанавливались, и повторных предупреждений в прогоне не было.

## Воспроизведение

```powershell
.\Tools\Build.ps1
.\Tools\Build.ps1 -Game
.\Tools\Test.ps1 -Mode Unit
.\Tools\Test.ps1 -Mode ArenaNetwork -PacketLagMs 75 -PacketLoss 2
.\Tools\CaptureArenaDemo.ps1
.\Tools\Test.ps1 -Mode Network
.\Tools\Test.ps1 -Mode Visual
.\Tools\Test.ps1 -Mode Ragdoll
.\Tools\Test.ps1 -Mode Ragdoll -PacketLagMs 75 -PacketLoss 2
.\Tools\Test.ps1 -Mode RagdollVisual
.\Tools\Test.ps1 -Mode Limbs
.\Tools\Test.ps1 -Mode Limbs -FrameRate 30
```

`Unit` запускает Unreal Automation:

- `MessControl.Gameplay.SevenDayVictory`: создание заданий, отсутствие соседних одинаковых событий, завершение каждого дня, защита от повторного завершения, победа и сброс забега.
- `MessControl.Gameplay.TimeoutAndLoss`: штраф за оставшиеся задачи, проигрыш и очистка акторов.
- `MessControl.Gameplay.WorkValidationAndCooperation`: неверный инструмент, дистанция, высота, фаза дня, сложение работы двух игроков и ограничение переданного времени.
- `MessControl.Gameplay.RunRules`: сохранённый DA, применение нестандартного профиля при старте, победа после двух дней, предел лечения 40, отказ третьему игроку при лимите два, неизменность правил до перезапуска и безопасные значения при отсутствии профиля.
- `MessControl.Physics.RigAndTuning`: сохранённый скелет, morph targets, материалы, семь тел / шесть суставов, сохранённые жёсткие ограничения суставов, новые звуки, защита параметров от NaN и выхода за диапазон.
- `MessControl.Physics.AuthorityAndActionGates`: частота ударов, попадание в ближайшего зуба, включение ragdoll, запрет работы/удара лёжа, отказ подъёма без пола.

`Network` поднимает listen server и три клиента на localhost. Автоматические игроки используют стандартное сетевое движение и RPC начала/конца работы. Каждый процесс должен увидеть четыре PlayerState и изменение общего прогресса. FPS ограничен 60, клиенты завершают проверку раньше хоста. Это проверяет настоящий сетевой путь, но не заменяет испытания на WAN с задержкой и потерей пакетов.

`Ragdoll` дополнительно сталкивает игроков через обычный запрос удара, проверяет наблюдение падений/подъёмов на сервере и клиентах, затем возвращает ботов к работе над заданиями. Допустимы параметры эмуляции `-PacketLagMs` и `-PacketLoss`; они передаются каждому процессу. Проверка проходит на localhost и не заменяет WAN-тест.

`Limbs` — отдельная регрессия вращения рук: 33 секунды покоя, бега с поворотами, чистки, падения/подъёма и F1-предпросмотра. Измеряет локальные повороты плеч и кистей, скорость в покое и возврат к исходной стойке. Провал: суммарно ≥0,15 с с поворотом >135° или нечисловым углом, скорость в покое ≥120°/с, отклонение от начальной стойки ≥35° либо отсутствие подъёма. На прежней версии после подъёма угол доходил до 180° и оставался неверным более 18 секунд. Логи: `Saved/Logs/Limbs30.log` / `Limbs60.log`; параметр `-FrameRate` ограничивает FPS, а не задаёт фиксированный шаг симуляции. Для кадров тот же запуск Unreal можно выполнить с `-MCLimbs -MCLimbsCapture -RenderOffscreen` вместо `-nullrhi`: снимки сохраняются в `Saved/LimbFrames`.

`Visual` сохраняет `Artifacts/Unreal_Gameplay.png`, `Unreal_AnimationLab.png`, `Unreal_PhysicsLab.png`. `RagdollVisual` сохраняет кадры Rig / Ragdoll / GetUp / Recovered, переключая наблюдение на тренировочного зуба после удара. Снимки проверяются визуально; сам скрипт не оценивает композицию или экспозицию. `Artifacts/Mouth_ArtPreview.png` — отдельный рендер Blender, а не снимок игры.

Отчёт Automation: `Saved/TestReports/index.json`. Логи: `Saved/Logs/Automation.log`, `Network0.log`…`Network3.log`, `Ragdoll0.log`…`Ragdoll3.log`, `Visual.log`, `RagdollVisual.log`. В репозитории остаются исходники проверок и снимки, а локальные логи исключены из Git.

## Перед расширением игры

Вручную проверить клавиатуру, геймпад и потерю фокуса; подключение с другого компьютера; поздний вход и выход клиента; угол камеры у всех границ; конкурирующую работу четырёх игроков на одной цели; сохранение и сброс ползунков F1. WAN, Steam/EOS, упаковка для распространения и полноценный игровой баланс не подтверждаются автоматическими проверками этого прототипа.
